const sumAll = function(x,y) {

    //check input
    if(Number.isInteger(x) && Number.isInteger(y) && x >= 0 && y >= 0){
        console.log("i work");
    }
    //min and max
    //loop for sum
    //return
}

module.exports = sumAll
